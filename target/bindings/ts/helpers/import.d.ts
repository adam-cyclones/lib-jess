/**
 * Appends the Tess file extname if not existing in path
*/
export declare function import_jess_sync(path: string): string;
/**
 * Appends the Tess file extname if not existing in path
*/
export declare function import_tess_sync(path: string): string;
